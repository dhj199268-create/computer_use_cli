# Computer Use CLI Configuration

# Delay between each operation in seconds
PAUSE=0.1

# Enable/disable fail-safe (move mouse to top-left to abort)
FAILSAFE=true

# Minimum duration for mouse movements in seconds
MINIMUM_DURATION=0.0

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
